import { Card, CardContent } from "@/components/ui/card";

/**
 * STUB — to be implemented by Wave 4 agent B.
 *
 * Must render /api/v1/sectors as two columns: inflows on the left,
 * outflows on the right. Sort each by abs(delta) descending. Pure
 * display, no filters.
 */
export async function SectorsTab() {
  return (
    <Card>
      <CardContent className="py-8 text-center text-sm text-muted-foreground">
        Sectors tab — coming online shortly.
      </CardContent>
    </Card>
  );
}
